# content/artists/zol/nav.yaml
items:
  - label: "Home"
    ref: "."                      # content/artists/zol/index.yaml

  - label: "Music"
    ref: "music"                  # content/artists/zol/music/index.yaml (or virtual)
    auto_children: from_subpages
    # Dropdown built from `nav: true` subpages under ZØL's music node
    # e.g. Albums, Tracks, Live Sets

  - label: "About"
    ref: "pages/about"            # content/artists/zol/pages/about/index.yaml

  - label: "Store"
    ref: "../../server/pages/store"
    # Reuses the server-level store page, but from the artist-centric nav
